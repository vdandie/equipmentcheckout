<%= form_for(SignOut.find_by(request_id: request.id),
							html: { method: :delete },
							remote: true) do |f| %>
  							<%= f.submit "Undo Sign Out", class: "btn", data: { confirm: "You sure?"} %>
						<% end %>